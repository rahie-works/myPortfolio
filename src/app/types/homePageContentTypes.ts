export type ProfileInfoType = {
  buttonText: string;
  id: string;
  location: string;
  name: string;
  title: string;
};

export type SummaryInfoType = {
  id: string;
  portfolioTitle: string;
  summaryHeading: string[];
  summaryIntro: string[];
  summaryLinks: string[];
};
